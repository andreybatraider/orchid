# 🔧 Решение проблемы "Permission denied"

## Проблема
Ошибка `E:/ORCHID/.git: Permission denied` при выполнении `git init`

## Решение 1: Запустить PowerShell от имени администратора

1. Закройте текущий PowerShell
2. Нажмите `Win + X`
3. Выберите **"Windows PowerShell (Администратор)"** или **"Терминал (Администратор)"**
4. Перейдите в папку проекта:
   ```bash
   cd E:\ORCHID
   ```
5. Выполните команды:
   ```bash
   git init
   git add .
   git commit -m "Initial commit: ORCHID project with admin panel"
   git branch -M main
   git remote add origin https://github.com/andreybatraider/orchid.git
   git push -u origin main
   ```

## Решение 2: Создать репозиторий в другом месте

Если проблема сохраняется, можно:
1. Скопировать проект в другую папку (например, `C:\Projects\ORCHID`)
2. Создать там репозиторий
3. Запушить на GitHub

## Решение 3: Проверить антивирус

Антивирус может блокировать создание папки `.git`. Временно отключите защиту или добавьте папку `E:\ORCHID` в исключения.

## Решение 4: Использовать Git Bash

1. Установите Git for Windows (если еще не установлен)
2. Откройте **Git Bash**
3. Перейдите в папку:
   ```bash
   cd /e/ORCHID
   ```
4. Выполните команды Git

## ✅ Рекомендация

**Лучше всего**: Запустить PowerShell от имени администратора и выполнить команды там.

