# 🗄️ Создание базы данных на Vercel

## ❌ Проблема

Ошибка `EROFS: read-only file system` означает, что на Vercel нельзя сохранять данные в файлы. Нужна база данных!

## ✅ Решение: Создать Redis базу данных

### Вариант 1: Upstash Redis (Рекомендуется)

Upstash Redis - это самый простой способ, работает с нашим кодом!

#### Шаги:

1. **На странице "Create a database" найдите "Upstash"**
   - Описание: "Serverless DB (Redis, Vector, Queue, Search)"
   - Нажмите на стрелку справа или "Create"

2. **Выберите "Redis"**
   - На странице Upstash выберите "Redis Database"
   - Нажмите "Create"

3. **Настройте базу данных:**
   - **Name**: `orchid-redis` (или любое имя)
   - **Region**: выберите ближайший регион (например, `eu-west-1` для Европы)
   - **Type**: выберите "Free" для начала (бесплатно)
   - Нажмите "Create"

4. **Подключите к проекту:**
   - Upstash автоматически создаст переменные окружения
   - Или вручную добавьте:
     - `UPSTASH_REDIS_REST_URL`
     - `UPSTASH_REDIS_REST_TOKEN`

5. **Обновите код для работы с Upstash:**
   - Нужно будет немного изменить `lib/data.ts` для работы с Upstash REST API

---

### Вариант 2: Redis из Marketplace

1. **На странице "Create a database" найдите "Redis"**
   - Описание: "Serverless Redis"
   - Нажмите "Create"

2. **Следуйте инструкциям:**
   - Выберите регион
   - Создайте базу данных
   - Переменные окружения добавятся автоматически

---

### Вариант 3: Vercel KV (если доступен)

Если Vercel KV доступен в вашем регионе:

1. **Найдите "KV" в списке**
2. **Создайте базу данных:**
   - Name: `orchid-kv`
   - Region: ближайший регион
3. **Vercel автоматически добавит:**
   - `KV_REST_API_URL`
   - `KV_REST_API_TOKEN`

---

## 🔧 После создания базы данных

### Шаг 1: Обновить код (если используется Upstash)

Если вы создали Upstash Redis, нужно обновить `lib/data.ts` для работы с Upstash REST API.

### Шаг 2: Пересобрать проект

1. Перейдите в **Deployments**
2. Нажмите **"Redeploy"** для последнего деплоя
3. Дождитесь завершения сборки

### Шаг 3: Проверить работу

1. Откройте админ-панель: `/admin`
2. Попробуйте добавить турнир или видео
3. Данные должны сохраняться!

---

## 📝 Какой вариант выбрать?

**Рекомендую: Upstash Redis**
- ✅ Бесплатный план доступен
- ✅ Легко настроить
- ✅ Работает с нашим кодом (после небольшого обновления)
- ✅ Быстрый и надежный

---

## 🆘 Нужна помощь?

Если возникли проблемы:
1. Проверьте, что переменные окружения добавлены
2. Убедитесь, что проект пересобран
3. Проверьте логи в Vercel

---

## ✅ Готово!

После создания базы данных и пересборки проекта, ошибка `EROFS` исчезнет, и данные начнут сохраняться!

