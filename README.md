# ORCHID - Турнирное агентство

Сайт для турнирного агентства ORCHID с админ-панелью для управления портфолио и турнирами.

🌐 Сайт: [orchid-pi.vercel.app](https://orchid-pi.vercel.app)

## 🎯 Особенности

- ✅ **Локальное хранилище данных** - не требует внешнего API
- ✅ **Админ-панель** - полное управление контентом через веб-интерфейс
- ✅ **Автоматическая синхронизация** - данные сохраняются автоматически
- ✅ **Работает на Vercel** - использует Vercel KV для хранения данных

## 🚀 Быстрый старт

### Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. (Опционально) Создайте файл `.env.local` в корне проекта:
```env
ADMIN_PASSWORD=admin123
```

3. Запустите dev server:
```bash
npm run dev
```

4. Откройте браузер:
- Главная: `http://localhost:3000`
- Админ-панель: `http://localhost:3000/admin`
- Пароль по умолчанию: `admin123`

Данные будут сохраняться в файл `data/store.json`.

## 📦 Деплой на Vercel

### Шаг 1: Создание KV базы данных

1. Перейдите в настройки вашего проекта на Vercel
2. Выберите вкладку "Storage"
3. Нажмите "Create Database" → выберите "KV" (Redis)
4. Создайте базу данных

### Шаг 2: Environment Variables

Vercel автоматически добавит переменные для KV:
- `KV_REST_API_URL` (автоматически)
- `KV_REST_API_TOKEN` (автоматически)

**Добавьте вручную**:
- `ADMIN_PASSWORD` = ваш безопасный пароль

### Шаг 3: Деплой

1. Подключите репозиторий к Vercel
2. Vercel автоматически соберет и задеплоит проект
3. Готово! 🎉

Подробные инструкции см. в `VERCEL_SETUP.md`

## 🎨 Функционал

### Админ-панель

- ✅ Аутентификация через пароль
- ✅ Управление портфолио (CRUD)
- ✅ Управление турнирами (CRUD)
- ✅ Защищенные API routes

### Публичные страницы

- ✅ Главная страница
- ✅ Портфолио (видео)
- ✅ Турниры
- ✅ Услуги
- ✅ О нас

## 📁 Структура проекта

```
app/
  api/
    portfolio/route.ts          # API для получения портфолио
    tournaments/route.ts         # API для получения турниров
    admin/
      auth/route.ts              # API для аутентификации
      portfolio/route.ts         # API для управления портфолио (CRUD)
      tournaments/route.ts       # API для управления турнирами (CRUD)
  admin/
    page.tsx                     # Страница входа в админ-панель
    dashboard/page.tsx           # Главная страница админки
    portfolio/page.tsx           # Управление портфолио
    tournaments/page.tsx         # Управление турнирами
  portfolio/page.tsx             # Публичная страница портфолио
  tournament/page.tsx            # Публичная страница турниров
lib/
  api.ts                         # Функции для получения данных
  data.ts                        # Локальное хранилище данных
data/
  store.json                     # Файл с данными (для локальной разработки)
```

## 🔒 Безопасность

1. **Пароль админа**: Измените `ADMIN_PASSWORD` на безопасный пароль
2. **Environment Variables**: Не коммитьте `.env.local` в Git (уже в `.gitignore`)
3. **Данные**: На Vercel данные хранятся в защищенной KV базе данных

## 💾 Хранение данных

### Локальная разработка
- Данные сохраняются в файл `data/store.json`
- Файл создается автоматически при первом запуске
- Можно редактировать вручную или через админ-панель

### На Vercel (Production)
- Данные автоматически сохраняются в Vercel KV (Redis)
- Не требуется дополнительная настройка - просто создайте KV базу данных
- Данные персистентны между перезапусками

## 📚 Дополнительная документация

- `VERCEL_SETUP.md` - Подробная инструкция по настройке Vercel
- `.env.local` - Файл с переменными окружения (создайте вручную)

## 🛠 Технологии

- Next.js 15
- React 18
- TypeScript
- Tailwind CSS
- HeroUI
- Vercel KV (для хранения данных на Vercel)

## 📝 Лицензия

См. файл LICENSE

## 🆘 Поддержка

Если что-то не работает:
1. Проверьте логи в консоли браузера
2. Проверьте логи сборки в Vercel
3. Убедитесь, что все Environment Variables установлены
4. Для Vercel: убедитесь, что создана KV база данных
