# Инструкция по деплою на Vercel

## 🚀 Быстрый деплой

### Шаг 1: Подготовка к деплою

Проект готов к деплою! Все файлы на месте.

### Шаг 2: Пуш на GitHub

1. Создайте репозиторий на GitHub (если еще не создан)
2. Выполните команды:

```bash
git init
git add .
git commit -m "Initial commit: ORCHID project with admin panel"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### Шаг 3: Деплой на Vercel

1. Перейдите на https://vercel.com
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"Add New Project"** (Добавить новый проект)
4. Выберите ваш репозиторий из GitHub
5. Vercel автоматически определит Next.js проект
6. Нажмите **"Deploy"**

### Шаг 4: Настройка Environment Variables

После первого деплоя:

1. Перейдите в настройки проекта на Vercel
2. Выберите **"Settings"** → **"Environment Variables"**
3. Добавьте переменную:
   - `ADMIN_PASSWORD` = `ваш_безопасный_пароль`
4. Выберите окружения: **Production**, **Preview**, **Development**
5. Нажмите **"Save"**
6. Перейдите в **"Deployments"** и нажмите **"Redeploy"** для последнего деплоя

### Шаг 5: Проверка работы

После деплоя проверьте:
- ✅ Главная страница открывается
- ✅ Страница портфолио работает (пока пустая)
- ✅ Страница турниров работает (пока пустая)
- ✅ Админ-панель доступна по адресу: `https://ваш-домен.vercel.app/admin`

## ⚠️ Важно: База данных

**На данный момент база данных не настроена.**

Это значит:
- ✅ Сайт будет работать
- ✅ Админ-панель будет доступна
- ❌ Данные НЕ будут сохраняться между перезапусками
- ❌ Изменения в админ-панели будут теряться

### Что делать дальше?

После успешного деплоя:
1. Создайте Vercel KV базу данных (см. `VERCEL_DATABASE_SETUP.md`)
2. Данные начнут сохраняться автоматически

Или можно использовать проект как есть для тестирования.

## 🔒 Безопасность

⚠️ **Обязательно измените `ADMIN_PASSWORD`** на безопасный пароль перед использованием в продакшене!

## 📚 Дополнительная документация

- `VERCEL_SETUP.md` - Подробная инструкция по настройке
- `VERCEL_DATABASE_SETUP.md` - Инструкция по настройке базы данных
- `README.md` - Общая информация о проекте

## 🆘 Решение проблем

### Ошибки при сборке

Если есть ошибки при сборке:
1. Проверьте логи в Vercel
2. Убедитесь, что все зависимости установлены
3. Проверьте, что TypeScript компилируется без ошибок

### Данные не сохраняются

Это нормально! Без базы данных данные не сохраняются. Настройте Vercel KV (см. `VERCEL_DATABASE_SETUP.md`).

### Админ-панель не работает

Убедитесь, что:
1. `ADMIN_PASSWORD` добавлен в Environment Variables
2. Проект пересобран после добавления переменных
3. Пароль введен правильно

## ✅ Готово!

После деплоя ваш сайт будет доступен по адресу:
`https://ваш-проект.vercel.app`

Удачи! 🎉

