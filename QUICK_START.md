# 🚀 Быстрый старт: Деплой на Vercel

## Шаг 1: Пуш на GitHub

### 1.1. Инициализация Git (если еще не сделано)

```bash
git init
```

### 1.2. Добавление файлов

```bash
git add .
```

### 1.3. Первый коммит

```bash
git commit -m "Initial commit: ORCHID project with admin panel"
```

### 1.4. Создание репозитория на GitHub

1. Перейдите на https://github.com
2. Нажмите **"New repository"** (Создать репозиторий)
3. Дайте имя репозиторию (например: `orchid-website`)
4. Не добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите **"Create repository"**

### 1.5. Подключение к GitHub и пуш

```bash
git branch -M main
git remote add origin https://github.com/ВАШ-USERNAME/ВАШ-РЕПОЗИТОРИЙ.git
git push -u origin main
```

Замените:
- `ВАШ-USERNAME` - ваш username на GitHub
- `ВАШ-РЕПОЗИТОРИЙ` - имя вашего репозитория

## Шаг 2: Деплой на Vercel

### 2.1. Подключение репозитория

1. Перейдите на https://vercel.com
2. Войдите в аккаунт (или создайте новый через GitHub)
3. Нажмите **"Add New Project"** (Добавить новый проект)
4. Выберите ваш репозиторий из списка
5. Нажмите **"Import"**

### 2.2. Настройка проекта

Vercel автоматически определит Next.js проект. Нажмите **"Deploy"**.

### 2.3. Добавление Environment Variables

После первого деплоя:

1. Перейдите в настройки проекта: **Settings** → **Environment Variables**
2. Добавьте переменную:
   - **Key**: `ADMIN_PASSWORD`
   - **Value**: `ваш_безопасный_пароль` (измените на безопасный!)
   - **Environment**: выберите все (Production, Preview, Development)
3. Нажмите **"Save"**
4. Перейдите в **Deployments**
5. Нажмите на три точки (⋯) у последнего деплоя
6. Выберите **"Redeploy"**

## Шаг 3: Проверка

После деплоя ваш сайт будет доступен по адресу:
```
https://ваш-проект.vercel.app
```

Проверьте:
- ✅ Главная страница работает
- ✅ Админ-панель доступна: `https://ваш-проект.vercel.app/admin`
- ✅ Можно войти с паролем из `ADMIN_PASSWORD`

## ⚠️ Важно

### База данных

**На данный момент база данных не настроена.** Это значит:
- ✅ Сайт работает
- ✅ Админ-панель доступна
- ❌ Данные НЕ сохраняются (изменения теряются при перезапуске)

**Для сохранения данных:**
1. Создайте Vercel KV базу данных (см. `VERCEL_DATABASE_SETUP.md`)
2. Данные начнут сохраняться автоматически

### Безопасность

⚠️ **Обязательно измените `ADMIN_PASSWORD`** на безопасный пароль!

## 📚 Дополнительная информация

- `DEPLOY.md` - Подробная инструкция по деплою
- `VERCEL_SETUP.md` - Настройка Vercel
- `VERCEL_DATABASE_SETUP.md` - Настройка базы данных
- `README.md` - Общая информация

## 🆘 Проблемы?

### Ошибки при деплое

1. Проверьте логи в Vercel
2. Убедитесь, что все файлы закоммичены
3. Проверьте, что `package.json` содержит все зависимости

### Админ-панель не работает

1. Убедитесь, что `ADMIN_PASSWORD` добавлен в Environment Variables
2. Пересоберите проект (Redeploy)
3. Проверьте пароль

### Данные не сохраняются

Это нормально без базы данных! Настройте Vercel KV (см. `VERCEL_DATABASE_SETUP.md`).

## ✅ Готово!

Ваш сайт задеплоен и готов к использованию! 🎉

После настройки базы данных все изменения будут сохраняться автоматически.

