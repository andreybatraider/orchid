# 🗄️ Инструкция: Создание базы данных на Vercel

## ✅ Что нужно сделать СЕЙЧАС:

### Шаг 1: Создайте Redis базу данных

На странице "Create a database":

1. **Найдите "Redis"** в разделе "Marketplace Database Providers"
   - Или найдите **"Upstash"** → выберите **"Redis Database"**

2. **Нажмите "Create"**

3. **Настройте базу данных:**
   - **Name**: `orchid-redis`
   - **Region**: выберите ближайший регион
   - **Plan**: выберите "Free" (если доступен)

4. **Создайте базу данных**

### Шаг 2: Проверьте переменные окружения

После создания базы данных Vercel автоматически добавит переменные окружения.

**Проверьте в Settings → Environment Variables:**

Если используется **Vercel KV** или **Redis из Marketplace**:
- `KV_REST_API_URL`
- `KV_REST_API_TOKEN`

Если используется **Upstash**:
- `UPSTASH_REDIS_REST_URL`
- `UPSTASH_REDIS_REST_TOKEN`

### Шаг 3: Пересоберите проект

1. Перейдите в **"Deployments"**
2. Нажмите на три точки (⋯) у последнего деплоя
3. Выберите **"Redeploy"**
4. Дождитесь завершения сборки

### Шаг 4: Проверьте работу

1. Откройте админ-панель: `/admin`
2. Попробуйте добавить турнир или видео
3. Данные должны сохраняться! ✅

---

## 🔧 Если что-то не работает

### Вариант 1: Используйте Upstash Redis

1. Создайте базу данных через **Upstash**
2. После создания вы получите:
   - `UPSTASH_REDIS_REST_URL`
   - `UPSTASH_REDIS_REST_TOKEN`
3. Код уже поддерживает Upstash! ✅

### Вариант 2: Используйте Vercel KV

1. Создайте базу данных **KV** (если доступна)
2. После создания вы получите:
   - `KV_REST_API_URL`
   - `KV_REST_API_TOKEN`
3. Код уже поддерживает Vercel KV! ✅

---

## 📝 Какие переменные окружения нужны?

### Для Vercel KV:
- `KV_REST_API_URL`
- `KV_REST_API_TOKEN`

### Для Upstash Redis:
- `UPSTASH_REDIS_REST_URL`
- `UPSTASH_REDIS_REST_TOKEN`

**Код автоматически определит, какой вариант использовать!**

---

## ✅ Готово!

После создания базы данных и пересборки:
- ✅ Ошибка `EROFS` исчезнет
- ✅ Данные начнут сохраняться
- ✅ Админ-панель будет работать полностью

---

## 🆘 Нужна помощь?

Если переменные окружения отличаются от ожидаемых:
1. Сообщите мне названия переменных
2. Я обновлю код для работы с вашей базой данных

