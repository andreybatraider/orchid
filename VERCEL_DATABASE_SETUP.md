# Настройка базы данных на Vercel

## 🎯 Варианты баз данных на Vercel

Проект поддерживает два варианта хранения данных:

### 1. Vercel KV (Redis) - УЖЕ НАСТРОЕНО ✅

Это самый простой вариант для данного проекта. KV - это быстрая база данных типа ключ-значение.

#### Как создать Vercel KV:

1. **Перейдите в ваш проект на Vercel**
   - Откройте https://vercel.com/dashboard
   - Выберите ваш проект

2. **Создайте KV базу данных**
   - Перейдите во вкладку **"Storage"** (Хранилище)
   - Нажмите **"Create Database"** (Создать базу данных)
   - Выберите **"KV"** (Redis)
   - Дайте имя базе данных (например: `orchid-db`)
   - Выберите регион (ближайший к вашим пользователям)
   - Нажмите **"Create"**

3. **Vercel автоматически добавит переменные окружения**
   После создания KV базы данных, Vercel автоматически добавит:
   - `KV_REST_API_URL`
   - `KV_REST_API_TOKEN`
   - `KV_REST_API_READ_ONLY_TOKEN`

4. **Добавьте пароль админа**
   - Перейдите в **"Settings"** → **"Environment Variables"**
   - Добавьте: `ADMIN_PASSWORD` = `ваш_безопасный_пароль`

5. **Пересоберите проект**
   - Перейдите в **"Deployments"**
   - Нажмите на последний деплой
   - Нажмите **"Redeploy"**

#### ✅ Готово!

После этого ваш проект автоматически будет использовать Vercel KV для хранения данных. Никаких дополнительных настроек не требуется!

---

### 2. Vercel Postgres (Опционально)

Если вам нужна более мощная SQL база данных, можно использовать Vercel Postgres.

#### Преимущества Postgres:
- ✅ SQL запросы
- ✅ Связи между таблицами
- ✅ Более сложные запросы
- ✅ Лучше для больших объемов данных

#### Как создать Vercel Postgres:

1. **Перейдите в ваш проект на Vercel**
   - Откройте https://vercel.com/dashboard
   - Выберите ваш проект

2. **Создайте Postgres базу данных**
   - Перейдите во вкладку **"Storage"**
   - Нажмите **"Create Database"**
   - Выберите **"Postgres"**
   - Дайте имя базе данных
   - Выберите регион
   - Нажмите **"Create"**

3. **Vercel автоматически добавит переменные окружения**
   После создания Postgres базы данных, Vercel автоматически добавит:
   - `POSTGRES_URL`
   - `POSTGRES_PRISMA_URL`
   - `POSTGRES_URL_NON_POOLING`
   - И другие переменные

4. **Обновите код проекта**
   Нужно будет обновить `lib/data.ts` для работы с Postgres вместо KV.

#### ⚠️ Важно:
Для использования Postgres нужно:
- Установить пакет для работы с Postgres (например, `@vercel/postgres` или `pg`)
- Обновить функции в `lib/data.ts` для работы с SQL
- Создать таблицы в базе данных

---

## 📊 Сравнение вариантов

| Характеристика | Vercel KV | Vercel Postgres |
|----------------|-----------|-----------------|
| Тип базы данных | Redis (ключ-значение) | PostgreSQL (SQL) |
| Сложность настройки | ✅ Очень простая | ⚠️ Средняя |
| Уже настроено | ✅ Да | ❌ Нет |
| Подходит для этого проекта | ✅ Да | ⚠️ Избыточно |
| Стоимость | Бесплатно (до лимитов) | Бесплатно (до лимитов) |

## 🎯 Рекомендация

Для вашего проекта (портфолио и турниры) **Vercel KV идеально подходит**:
- ✅ Простая структура данных
- ✅ Быстрый доступ
- ✅ Легкая настройка
- ✅ Уже настроено в проекте

**Используйте Vercel KV** - это самый простой и быстрый вариант!

---

## 🚀 Быстрый старт с Vercel KV

1. Создайте KV базу данных в Vercel (см. инструкцию выше)
2. Добавьте `ADMIN_PASSWORD` в Environment Variables
3. Пересоберите проект
4. Готово! 🎉

Данные автоматически начнут сохраняться в KV базе данных.

---

## ❓ Нужна помощь?

Если вам нужна помощь с настройкой Postgres или есть вопросы по KV, дайте знать!

