# Инструкция по настройке для Vercel

## 🎯 Важно!

Проект теперь использует **локальное хранилище данных** вместо внешнего API. Данные хранятся:
- **Локально**: в файле `data/store.json` (для разработки)
- **На Vercel**: в Vercel KV (Redis) - автоматически при наличии переменных окружения

## 📦 Установка зависимостей

Установите зависимости (включая @vercel/kv для работы на Vercel):

```bash
npm install
```

## 🔧 Настройка для локальной разработки

### 1. Создание файла .env.local (опционально)

Файл `.env.local` нужен только для настройки админ-панели:

```env
ADMIN_PASSWORD=admin123
```

⚠️ **Важно**: Измените `ADMIN_PASSWORD` на более безопасный пароль!

### 2. Запуск локально

```bash
npm run dev
```

Данные будут сохраняться в файл `data/store.json`.

## 🚀 Настройка для Vercel

### 1. Создание Vercel KV базы данных

1. Перейдите в настройки вашего проекта на Vercel
2. Выберите вкладку "Storage"
3. Нажмите "Create Database" → выберите "KV" (Redis)
4. Создайте базу данных (это бесплатно для небольших проектов)

### 2. Добавление Environment Variables в Vercel

После создания KV базы данных, Vercel автоматически добавит следующие переменные:
- `KV_REST_API_URL`
- `KV_REST_API_TOKEN`
- `KV_REST_API_READ_ONLY_TOKEN`

**Дополнительно добавьте**:
- `ADMIN_PASSWORD` = `ваш_безопасный_пароль`

⚠️ **Важно для Production**: 
- Убедитесь, что все переменные добавлены для окружений: **Production**, **Preview**, и **Development**
- После добавления переменных, **обязательно пересоберите проект** (Redeploy)
- Измените `ADMIN_PASSWORD` на безопасный пароль перед деплоем!

### 3. Деплой на Vercel

1. Подключите ваш репозиторий к Vercel
2. Vercel автоматически определит Next.js проект
3. Создайте KV базу данных (см. выше)
4. Добавьте `ADMIN_PASSWORD` в Environment Variables
5. Нажмите "Deploy"

## 📝 Как это работает

### Локальная разработка
- Данные сохраняются в файл `data/store.json`
- Файл создается автоматически при первом запуске
- Можно коммитить `data/store.json` в Git для начальных данных (опционально)

### На Vercel (Production)
- Данные автоматически сохраняются в Vercel KV (Redis)
- Не требуется дополнительная настройка - просто создайте KV базу данных
- Данные персистентны между перезапусками

## 🔒 Безопасность

⚠️ **Рекомендации по безопасности**:

1. Используйте сложный пароль для `ADMIN_PASSWORD`
2. Не коммитьте `.env.local` в Git (уже добавлен в `.gitignore`)
3. Регулярно обновляйте пароль
4. Рассмотрите возможность добавления дополнительной аутентификации (например, через NextAuth.js)

## 📍 Доступ к админ-панели

После деплоя, админ-панель будет доступна по адресу:
- `https://ваш-домен.vercel.app/admin` - страница входа
- `https://ваш-домен.vercel.app/admin/dashboard` - главная страница админки
- `https://ваш-домен.vercel.app/admin/portfolio` - управление портфолио
- `https://ваш-домен.vercel.app/admin/tournaments` - управление турнирами

Пароль для входа: значение из переменной `ADMIN_PASSWORD` (по умолчанию: `admin123`)

## ✅ Проверка работы

После деплоя проверьте:
1. Главная страница загружается корректно
2. Страница портфолио отображает данные (изначально пусто)
3. Страница турниров отображает данные (изначально пусто)
4. Админ-панель доступна и работает
5. Можно добавлять/редактировать/удалять записи через админ-панель
6. Данные сохраняются и отображаются после перезагрузки

## 🔧 Решение проблем

Если что-то не работает:

1. **Ошибки при сохранении данных на Vercel**: 
   - Убедитесь, что создали KV базу данных
   - Проверьте, что переменные `KV_REST_API_URL` и `KV_REST_API_TOKEN` установлены

2. **Ошибки авторизации**: 
   - Убедитесь, что `ADMIN_PASSWORD` установлен корректно в Vercel

3. **Данные не сохраняются локально**: 
   - Убедитесь, что у вас есть права на запись в директорию `data/`
   - Проверьте, что файл `data/store.json` создается

4. **Ошибки сборки**: 
   - Проверьте логи сборки в Vercel
   - Убедитесь, что установлены все зависимости (`npm install`)

## 📁 Структура проекта

```
app/
  api/
    portfolio/route.ts          # API для получения портфолио
    tournaments/route.ts         # API для получения турниров
    admin/
      auth/route.ts              # API для аутентификации
      portfolio/route.ts         # API для управления портфолио (CRUD)
      tournaments/route.ts       # API для управления турнирами (CRUD)
  admin/
    page.tsx                     # Страница входа в админ-панель
    dashboard/page.tsx           # Главная страница админки
    portfolio/page.tsx           # Управление портфолио
    tournaments/page.tsx         # Управление турнирами
  portfolio/page.tsx             # Публичная страница портфолио
  tournament/page.tsx            # Публичная страница турниров
lib/
  api.ts                         # Функции для получения данных (используют lib/data.ts)
  data.ts                        # Локальное хранилище данных (JSON файл или Vercel KV)
data/
  store.json                     # Файл с данными (для локальной разработки)
```

## 🎯 API Endpoints

### Публичные API:
- `GET /api/portfolio` - Получить список видео портфолио
- `GET /api/tournaments` - Получить список турниров

### Админ API (требуется аутентификация):
- `GET /api/admin/auth` - Проверить статус аутентификации
- `POST /api/admin/auth` - Войти в админ-панель
- `DELETE /api/admin/auth` - Выйти из админ-панели
- `GET /api/admin/portfolio` - Получить все видео (для админа)
- `POST /api/admin/portfolio` - Создать новое видео
- `PUT /api/admin/portfolio` - Обновить видео
- `DELETE /api/admin/portfolio?id={id}` - Удалить видео
- `GET /api/admin/tournaments` - Получить все турниры (для админа)
- `POST /api/admin/tournaments` - Создать новый турнир
- `PUT /api/admin/tournaments` - Обновить турнир
- `DELETE /api/admin/tournaments?id={id}` - Удалить турнир

## 💡 Примечания

- **Внешний API больше не используется** - все данные хранятся локально
- Для начальных данных можно отредактировать `data/store.json` вручную
- На Vercel данные автоматически мигрируют в KV базу данных
- Данные из файла `data/store.json` не автоматически переносятся в KV - нужно добавить их через админ-панель
