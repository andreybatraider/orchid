# 🔧 Настройка REDIS_URL на Vercel

## ✅ База данных создана!

Вы создали Redis базу данных `orchid-redis` на Vercel. Теперь нужно добавить переменную `REDIS_URL` в проект.

## 📝 Шаг 1: Добавьте REDIS_URL в Environment Variables

### В проекте на Vercel:

1. **Откройте ваш проект** `orchid` (не `orchid-omqh`)
2. Перейдите в **Settings** → **Environment Variables**
3. Нажмите **"Add New"**
4. Заполните:
   - **Key**: `REDIS_URL`
   - **Value**: `redis://default:6MpUBXXyj8CkYQk4eLcq62NM5l5QdcdK@redis-17493.c277.us-east-1-3.ec2.redns.redis-cloud.com:17493`
   - **Environment**: выберите все (Production, Preview, Development)
5. Нажмите **"Save"**

## ⚠️ Важно:

- **Не добавляйте `REDIS_URL` в публичный репозиторий!**
- Переменная уже должна быть в Vercel после создания базы данных
- Проверьте, что переменная добавлена для всех окружений

## 🔄 Шаг 2: Пересоберите проект

После добавления переменной:

1. Перейдите в **Deployments**
2. Нажмите на три точки (⋯) у последнего деплоя
3. Выберите **"Redeploy"**
4. Дождитесь завершения сборки

## ✅ Шаг 3: Проверьте работу

1. Откройте админ-панель: `/admin`
2. Попробуйте добавить турнир или видео
3. Данные должны сохраняться! ✅

## 🎯 Что изменилось в коде:

- ✅ Добавлена поддержка `REDIS_URL` через библиотеку `ioredis`
- ✅ Код автоматически определяет, какой вариант Redis использовать:
  - `KV_REST_API_URL` + `KV_REST_API_TOKEN` (Vercel KV)
  - `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` (Upstash)
  - `REDIS_URL` (стандартный Redis) ← **ВАШ ВАРИАНТ**

## 📝 Альтернатива: Проверьте автоматическое подключение

Vercel может автоматически подключить базу данных к проекту:

1. На странице базы данных `orchid-redis`
2. Нажмите **"Connect Project"**
3. Выберите проект `orchid`
4. Vercel автоматически добавит переменную `REDIS_URL`

## ✅ Готово!

После настройки `REDIS_URL` и пересборки проекта:
- ✅ Ошибка `EROFS` исчезнет
- ✅ Данные начнут сохраняться в Redis
- ✅ Админ-панель будет работать полностью

---

## 🆘 Если что-то не работает:

1. **Проверьте переменные окружения:**
   - Убедитесь, что `REDIS_URL` добавлена
   - Проверьте, что значение правильное
   - Убедитесь, что переменная для всех окружений

2. **Проверьте подключение к проекту:**
   - На странице базы данных нажмите **"Connect Project"**
   - Выберите проект `orchid`

3. **Проверьте логи:**
   - В Vercel перейдите в **Logs**
   - Проверьте, нет ли ошибок подключения к Redis

4. **Пересоберите проект:**
   - Убедитесь, что проект пересобран после добавления переменной

---

## 📚 Дополнительно:

- Код поддерживает три варианта Redis: Vercel KV, Upstash, и стандартный Redis
- Вы используете стандартный Redis через `REDIS_URL`
- Данные сохраняются в ключ `orchid-data` в Redis

